<% content_for :title do %>
<%= @page.title %> | <%= Rails.application.config.site_name %>
<% end %>

<% content_for :meta do %>
  <%# `meta_tags` runs `sanitize_meta_description` on the value, so it is
      safe to hand the raw markdown body — markers, HTML tags, links, and
      code fences are stripped and the result is truncated for previews. %>
  <%= meta_tags(title: @page.title, description: @page.markdown_body) %>
<% end %>

<article class="max-w-2xl mx-auto">
    <header class="mb-8">
        <h1 class="text-3xl md:text-4xl font-bold text-gray-900 dark:text-white mb-4">
            <%= @page.title %>
        </h1>

        <div class="flex flex-col sm:flex-row sm:items-center text-gray-600 dark:text-gray-300 gap-4">
            <% if authenticated? %>
                <%= link_to "Edit", edit_page_path(@page),
                        class: "text-blue-600 dark:text-blue-400 hover:text-blue-800 dark:hover:text-blue-300 font-medium" %>
            <% end %>
        </div>
    </header>

<div class="text-gray-700 dark:text-gray-200 max-w-none mb-8 leading-relaxed">
    <%= @page.rendered_body.html_safe %>
</div>

</article>
